<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Тихий старт: без дампа конфигурации в stderr -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <!-- Компактный префикс: только время и уровень, без thread/logger. -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level | %msg%n%ex</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
